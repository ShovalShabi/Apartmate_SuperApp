package superapp.data;

/**
 * The UserRole enum represents the roles that a user can have within the application.
 * It defines three roles: ADMIN, SUPERAPP_USER, and MINIAPP_USER.
 */
public enum UserRole {
    ADMIN,
    SUPERAPP_USER,
    MINIAPP_USER
}
